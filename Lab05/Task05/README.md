# Lab05 - Task 05

In this task we will run an example that reads an image and the parameters we need to define the region of interest, and then writes this region to a image file. You have to build the example coded in the file ITK_DIR/Examples/IO/ImageReadRegionOfInterestWrite.cxx . Run the program with an image from ’ ITK_DIR/Examples/Data ’.
